import RevealPresentation2 from '@/components/RevealPresentation2';
import { Metadata } from 'next';

export const metadata: Metadata = {
    title: "Mefertation  | Prezentacja 2",
};

export default function Page() {
    return (
        <>
            <RevealPresentation2/>
        </>
    );
}
